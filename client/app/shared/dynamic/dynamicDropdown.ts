import {DynamicInput} from "./dynamicInput";
export class DynamicDropdown extends DynamicInput<string> {
  controlType = 'dropdown';
  options: {key: string, value: string}[] = [];

  constructor(options: {} = {}) {
    super(options);
    this.options = options['options'] || [];
  }
}
